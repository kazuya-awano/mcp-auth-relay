identity:
  name: "mcp_tool_list"
  author: "kazuya-awano"
  label:
    en_US: "List MCP Tools"
    zh_Hans: "List MCP Tools"
    pt_BR: "List MCP Tools"
    ja_JP: "List MCP Tools"
description:
  human:
    en_US: "List tools available on the configured MCP server."
    zh_Hans: "List tools available on the configured MCP server."
    pt_BR: "List tools available on the configured MCP server."
    ja_JP: "List tools available on the configured MCP server."
  llm: "List MCP tools from configured servers. It returns tool_ref values in server_id::tool_name format. Always use tool_ref with mcp_tool_call; do not call MCP tool names directly as Dify tools."
parameters:
  - name: server_id
    type: string
    label:
      en_US: Server ID
    human_description:
      en_US: "Optional server identifier to list tools only from one server."
    llm_description: "Optional server identifier to narrow tool listing."
    form: llm
extra:
  python:
    source: tools/tool_list.py
